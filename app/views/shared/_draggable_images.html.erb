<% if @pairing %>

  <div id="photo_container">
    <div class="photo">
      <div class="item">
        <%= image_tag @pairing.image_file1.file.url(:large), :class => "layer1", :style => "width: auto;" %>
        <div class="layer2">
          <%= image_tag @pairing.image_file2.file.url(:large) %>
        </div>
        <div class="overlay">
          <div class="draggable"></div>
        </div>
        <div class="row-fluid" id="photo_years">
          <div class="span4">
            <%= @pairing.image_file1.year_formatted %>
          </div>
          <div class="span4" style="text-align: center;">
            <p>
              <%= "#{@pairing_index} of #{@pairing_count}" %>
            </p>
          </div>
          <div class="span4">
            <%= @pairing.image_file2.year_formatted %>
          </div>
        </div>
      </div>

    </div>
    <div class="controls left"><%= link_to '&lsaquo;'.html_safe, previous_path(@pairing.id, @param_options) %></div>
    <div class="controls right"><%= link_to '&rsaquo;'.html_safe, next_path(@pairing.id, @param_options) %></div>
  </div>

  <div id="photo_content_container">
    <div class="row-fluid">
      <div class="span6">
        <h2><%= @pairing.title %></h2>

        <%= simple_format @pairing.description %>

      </div>
      <div class="span6">
        <div id="map"></div>    
      </div>
    </div>
  </div>
<% else %>
  <p>
    <%= t('.no_records') %>
  </p>
<% end %>

